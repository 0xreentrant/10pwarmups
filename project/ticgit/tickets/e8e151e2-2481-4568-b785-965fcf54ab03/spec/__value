Remove exit confirmation when leaving active training. Browser back/forward and in-screen back should navigate normally. REQUEST_EXIT transitions directly to home and clears session without recording abandoned attempts. Remove router block and index redirect-to-training guard. Remove ExitConfirmPopover from training route. Update routing and machine tests to assert immediate home navigation, no confirmation UI, and forward-to-training URL redirects to deck progress when no active session.